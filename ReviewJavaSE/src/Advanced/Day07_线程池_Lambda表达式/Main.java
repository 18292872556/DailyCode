package Advanced.Day07_线程池_Lambda表达式;

/**
 * Created with IntelliJ IDEA 2024.1.4.
 * jdk 17.0.12
 *
 * @Authur: xuexuezi
 * @Date: 2026/08/26/下午5:12
 * @Description:
 */
public class Main {
    // Day06 线程、同步、线程状态练习题
// 要求：每题独立完成；题目只给要求，不给实现步骤。
// 避免重复考察同一个知识点或完全相同的代码写法，并覆盖本章主要知识点。
    //之前35道题出的很差，写了两周，很多重复。让gpt重新出，不然太烦了

// ==================== Day07：线程池、Lambda表达式 ====================

// 01【线程通信】
// 创建一个共享资源对象，由两个线程分别执行不同任务：一个负责修改资源，一个负责读取资源。
// 使用 synchronized 保证两个线程操作共享资源时具有协调性，并通过输出体现两个线程处理的是同一份数据。


// 02【wait基本使用】
// 创建两个线程操作同一个锁对象：A获取锁后调用 wait() 进入等待，B随后获取同一把锁并通过 notify() 唤醒A。
// 输出A、B的关键执行位置，验证 wait() 会让当前线程等待以及被唤醒后继续执行。


// 03【wait与notify规则】
// 在上题基础上修改代码，验证 wait()/notify() 必须由同一个锁对象调用，且必须放在同步代码块或同步方法中。
// 分别尝试使用不同对象调用 wait/notify，并通过运行结果理解为什么不能这样使用。


// 04【wait与锁】
// 创建A、B两个线程：A在 synchronized 中调用 wait()；B随后尝试进入同一个 synchronized 代码块。
// 通过代码验证：A调用 wait() 后会释放锁，因此B可以获取锁；B结束后再通过 notify() 唤醒A。


// 05【notify与锁竞争】
// 创建A、B两个线程：A调用 wait()进入等待，B获取锁后调用 notify()，但不要立即释放锁。
// 通过第三个线程持续观察A状态，验证A被notify后可能先进入 BLOCKED，获得锁后才能继续执行。


// 06【notifyAll】
// 创建至少3个线程，让它们在同一个锁对象上依次调用 wait()进入等待。
// 再创建唤醒线程调用 notifyAll()，观察多个等待线程被唤醒后的执行情况。


// 07【生产者消费者】
// 使用一个共享的包子资源，实现“包子铺生产、吃货消费”：没有包子时吃货等待，有包子时吃货消费并通知包子铺。
// 要求使用 wait()/notify() 和 synchronized 实现完整的生产者消费者协作。


    /* 08【线程池基础】 */
// 使用 Executors 创建一个固定大小为2的线程池，创建一个 Runnable 任务并连续提交3次。
// 在任务中输出当前线程名称和任务编号，观察线程池中的线程如何执行多个任务，并在最后关闭线程池。


// 09【线程池线程复用】
// 创建一个固定大小为2的线程池，连续提交至少6个短任务。
// 每个任务输出任务编号和当前线程名称，根据输出验证：线程池中的有限线程可以反复执行多个任务。


// 10【submit与Runnable】
// 创建一个 Runnable 实现类作为任务，分别使用线程池的 submit() 提交多个任务。
// 要求任务中包含明显的执行耗时，通过输出观察提交任务后线程池线程的执行过程。


// 11【Lambda-Runnable标准格式】
// 不创建 Runnable 实现类，也不使用匿名内部类，直接使用 Lambda 创建 Runnable 任务。
// 使用 Lambda 的标准格式完成“启动线程并输出一段文字”的功能。


// 12【Lambda标准格式：自定义接口】
// 定义一个 Cook 接口，其中有且仅有一个无参无返回值的抽象方法 makeFood()。
// 定义 invokeCook(Cook cook) 方法，在 main 中使用 Lambda【标准格式】调用它并输出“吃饭啦！”。


// 13【Lambda标准格式：Comparator】
// 创建多个 Person 对象放入数组，使用 Arrays.sort() 按年龄升序排序。
// 不使用匿名内部类，使用 Lambda【标准格式】实现 Comparator<Person> 的排序规则并输出排序结果。


// 14【Lambda标准格式：有参有返回】
// 定义 Calculator 接口，其中有且仅有一个抽象方法 calc(int a, int b)，返回两个整数的计算结果。
// 使用 Lambda【标准格式】调用 invokeCalc()，完成120和130相加并输出结果。


// 15【Lambda省略格式：Runnable】
// 使用 Lambda 创建 Runnable 并启动线程，但必须使用 Lambda【省略格式】。
// 要求代码完成与第11题相同的功能，并体现参数类型、大括号等可以省略的规则。


// 16【Lambda省略格式：Comparator】
// 重新使用 Person 数组按年龄升序排序，但这次必须使用 Lambda【省略格式】。
// 尽可能省略参数类型、括号以及只有一条语句时的大括号、return和分号。


// 17【Lambda省略格式：自定义接口】
// 使用前面的 Cook 接口和 invokeCook() 方法，改用 Lambda【省略格式】完成调用。
// 要求不使用多余的大括号、return 等语法。


    /* 18【Lambda综合：标准→省略】 */
// 定义一个自定义函数式接口 Calculator，并使用 Lambda 标准格式完成两个整数相乘。
// 再将同一个 Lambda 改写为省略格式，要求你根据省略规则自行判断哪些部分可以删除。


// 19【Lambda使用前提：函数式接口】
// 分别定义两个接口：一个接口有且仅有一个抽象方法，另一个接口有两个抽象方法。
// 尝试分别使用 Lambda 创建它们的对象，通过编译结果验证 Lambda 对接口“有且仅有一个抽象方法”的要求。


// 20【Lambda综合 + 线程池】
// 创建一个固定大小为2的线程池，使用 Lambda 直接提交多个 Runnable 任务。
// 要求使用 Lambda【省略格式】，每个任务输出任务编号和执行它的线程名称，最后关闭线程池。

}
